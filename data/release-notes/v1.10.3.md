### Fixed

- Fixed an issue where adding a Bambu Lab printer by IP could fail with "No reply from …" even when the printer was online and reachable, because the check gave up too quickly. It now waits long enough, fills in the serial number and detects the model automatically, so you only need to enter the Access Code.

---

📖 **[Full changelog](https://github.com/TigerTag-Project/TigerTag-Studio-Manager/blob/main/CHANGELOG.md)** · ⬇️ Grab the installer for your OS from the **Assets** below (macOS `.dmg`, Windows `Setup .exe`, Linux `.AppImage`).
