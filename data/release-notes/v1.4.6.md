### Fixed

- Fixed an issue on Windows where the auto-updater returned a 404 and couldn't fetch the new version, caused by an installer filename mismatch. The Windows installer name is now standardised so auto-update works.

---

📖 **[Full changelog](https://github.com/TigerTag-Project/TigerTag-Studio-Manager/blob/main/CHANGELOG.md)** · ⬇️ Grab the installer for your OS from the **Assets** below (macOS `.dmg`, Windows `Setup .exe`, Linux `.AppImage`).
