### Changed

- **Auto storage and Auto unstorage merged into one "Auto-organize" toggle.** A single automation now auto-places new spools and frees emptied slots; lock a slot to make an exception. The setting is per-account and follows you across devices.
- Creality fan controls are now cards with −/+ 10% steppers, like the other printers, with one card per fan the printer actually has.
- **Tidier toolbar.** The Materials / Printers view switcher sits above the search bar in every view, and the "Spools not stored" header is more compact with hover info bubbles.
- **Cleaner Cam view.** The search bar and the Scan / Add-device buttons are hidden (they don't apply to the camera wall), and "Detach" moved up next to the other actions.

### Fixed

- Fixed an issue where switching between accounts while toggling the automation could clear every spool from its rack on the account you came back to, leaving the slot locks behind. Stale background updates from the previous account are now ignored, the automation only writes to the matching account, and Auto-organize never frees a spool in a locked slot.
- Fixed an issue where the RFID reader indicator showed a raw text key in its tooltip, and where the printer table did not line up with the materials table under the search bar.

---

📖 **[Full changelog](https://github.com/TigerTag-Project/TigerTag-Studio-Manager/blob/main/CHANGELOG.md)** · ⬇️ Grab the installer for your OS from the **Assets** below (macOS `.dmg`, Windows `Setup .exe`, Linux `.AppImage`).
