### Fixed

- Fixed an issue where the auto-update check could fail during transient GitHub hiccups. The app now talks to the correct project URL directly instead of relying on a redirect, halving the requests and removing a frequent failure point. The same fix applies to the "GitHub" buttons in the sidebar and the About dialog.

---

📖 **[Full changelog](https://github.com/TigerTag-Project/TigerTag-Studio-Manager/blob/main/CHANGELOG.md)** · ⬇️ Grab the installer for your OS from the **Assets** below (macOS `.dmg`, Windows `Setup .exe`, Linux `.AppImage`).
