### Fixed

- Fixed an issue where the Storage view flashed on every search keystroke, and where sweeping the mouse between racks that both contained matches produced a flash too, most noticeable on large inventories and a friend's read-only view. Both are now gone. As a side effect, each rack reserves a little space above the first shelf for the column-number labels (still shown only on hover), so racks are very slightly taller than before.

---

📖 **[Full changelog](https://github.com/TigerTag-Project/TigerTag-Studio-Manager/blob/main/CHANGELOG.md)** · ⬇️ Grab the installer for your OS from the **Assets** below (macOS `.dmg`, Windows `Setup .exe`, Linux `.AppImage`).
