### Added

- **Persistent logs.** The app now writes a rotating log file on each launch, making it far easier to diagnose start-up problems on a user's machine without running from a terminal. The log lives in the app's data folder on Windows, macOS and Linux.

---

📖 **[Full changelog](https://github.com/TigerTag-Project/TigerTag-Studio-Manager/blob/main/CHANGELOG.md)** · ⬇️ Grab the installer for your OS from the **Assets** below (macOS `.dmg`, Windows `Setup .exe`, Linux `.AppImage`).
