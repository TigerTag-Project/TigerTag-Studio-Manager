### Fixed

- Fixed an issue on Windows where the app could fail to start or break Google sign-in on machines with IPv6 disabled. The internal connection now uses a reliable address on every Windows version, so the app launches and Google sign-in works.

---

📖 **[Full changelog](https://github.com/TigerTag-Project/TigerTag-Studio-Manager/blob/main/CHANGELOG.md)** · ⬇️ Grab the installer for your OS from the **Assets** below (macOS `.dmg`, Windows `Setup .exe`, Linux `.AppImage`).
