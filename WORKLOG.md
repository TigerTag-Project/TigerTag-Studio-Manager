# Worklog — v1.10.13 (in progress)

## Added

## Changed
- Docs: `CLAUDE.md` now documents **where & how Firestore Security Rules are managed** (separate backend repo `TigerTag_Firebase_Backend/firestore.rules`, project `tigertag-connect`, deploy command, the owner-only / friends-gated cross-user-write / field-whitelist model) so a session doesn't need to open the 14 k rules file — and fixed the stale "rules implementation pending" note. `CLAUDE.md`

## Fixed

## Removed

## i18n
