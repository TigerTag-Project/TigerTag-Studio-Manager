# Worklog — v2.7.0

## Added
- **Right-click context menu** on any editable field (inputs, number/URL fields, textareas, contenteditable) with native Cut/Copy/Paste/Select-all, plus Copy on any selected text — `main.js` (`webContents 'context-menu'`).
- **Account stock summary stored server-side.** `recordStudioState` now writes the aggregate stock stats (value HT, weight g, currency, spool count) both to `users/{uid}/telemetry/studio` (Studio use) AND — since stock is a **general account fact** — to a `stock` object on the shared root `users/{uid}` doc (the entry TigerSystem / TigerHub read to roll up value across accounts without scanning every spool). Backend: 3 fields (`stockValueHt`, `stockWeightG`, `stockCurrency`) added to the `telemetry` `hasOnly` whitelist + deployed; the root `stock` object needs no rule change (root doc allows owner writes) — `renderer/inventory.js`, `firestore.rules`.
- **Create-list modal**: the ＋ / "New list" opens a modal with a required **name** + an optional **"Celebrating an event?"** occasion field + Cancel/Create, instead of creating an untitled list inline. The occasion shows under the list title. New `#createListOverlay` + `openCreateListModal`/`_submitCreateList` → `_createList(name, {occasion})` — `renderer/inventory.html`, `renderer/inventory.js`, `renderer/css/60-modals.css`.
- **Manage-list modal (edit) + hold-to-confirm delete.** The list header's pencil now opens the SAME modal in edit mode (title "Manage list", pre-filled name + occasion, "Save") with a **hold-to-confirm "Delete list"** button (per the destructive-action norm), replacing the inline rename + click-confirm delete. `openCreateListModal(editId)`, `#clmDelete` via `setupHoldToConfirm` — `renderer/inventory.js`, `renderer/inventory.html`, `renderer/css/60-modals.css`. New `listModalEditTitle`, `listModalSave` i18n.
- **"Add to a list"** is now also on the **grouped (deck) side card** — a third action button next to Product-info / Reorder, wired via the shared `.js-add-to-list` handler — `renderer/inventory.js` (`_renderGroupPanelContents`)
- **Lists view redesigned Amazon-style**: a **left sidebar** of the user's lists (name + count, selected highlighted, + "Create a list") and a **main column** with the selected list's items. A per-view **layout toggle (rows / grid)** in the header switches between Amazon-style **rows** (thumbnail · title · colour · price · yellow **Buy** pill · remove) and **grid** cards (persisted in `tigertag.listLayout`). Row/card click opens the product card — `renderer/inventory.js` (`renderListsView`, `_listRowHTML`, `_listCardHTML`), `renderer/css/70-detail-misc.css`.
- **Lists — Amazon-style shareable wishlists (Phase 1, in-app).** Create several named lists (optional occasion), add filaments from the **Material card** (spool detail toolbox) and the **Product card**, browse them in a new **"Lists" view** (a segment in the view selector). A list stores product identities (`users/{uid}/lists/{listId}`, `itemKeys` → product keyHash), so buy links / prices / images come for free and a list can hold a filament you don't own. Friends see your lists **live** in friend-view (read-only), same read policy as `products`. New `subscribeLists`/`subscribeFriendLists` + CRUD (`_createList`/`_renameList`/`_deleteList`/`_addToList`/`_removeFromList`), `renderListsView`, and a shared **"Add to a list"** popup (`_openAddToListMenu`) — `renderer/inventory.js`, `renderer/inventory.html`, `renderer/css/70-detail-misc.css`. Backend: new `users/{uid}/lists/{listId}` rules block mirroring `products` (owner/public/friend read, owner write + `hasOnly` whitelist) — `firestore.rules` (needs deploy). Phase 2 (public web link `cdn.tigertag.io/list/<token>` + reserve/bought) to follow.

## Changed
- Lists view items: a filament with **no price** shows an **"Add price"** action and one with **no buy link** shows an **"Add a link"** button (both open the reorder card straight into that editor); priced/linked items show the price + the "Buy" pill as before. Rows and grid — `renderer/inventory.js` (`_listRowHTML`, `_listCardHTML`, `_listRowFor`), `renderer/css/70-detail-misc.css`. Reuses `reorderAddPrice` / `reorderAddLink`.
- Lists **grid cards** now carry the same **Buy / Add-a-link** (cart) button as the row layout — a full-width bar at the bottom of each card (was rows-only) — `renderer/inventory.js` (`_listCardHTML`), `renderer/css/70-detail-misc.css`.
- **Buy buttons now show the shop's host** (cart icon + e.g. "amazon.fr", "atome3d.com") instead of a generic "Buy" — so you see where a link goes before clicking (trust + spot odd domains). Applied across the wishlist (rows + grid), the Product card, the grouped deck's buy button, and the Reorder card's shop button. New shared `_buyHost()` helper (hostname minus `www.`, falls back to "Buy"); the Material card's friend-view buy block already showed the host — `renderer/inventory.js` (`_buyHost`, `_listRowHTML`, `_listCardHTML`, `_renderGroupPanelContents`, `_renderProductCard`, reorder card). Icon-only cart buttons (Favorites grid/table) unchanged.
- Removing an item from a list is now **hold-to-confirm** (both the row trash button and the grid ✕) — a plain click no longer drops the product, so a misclick can't silently lose it. Reversible → 1200 ms — `renderer/inventory.js` (`renderListsView`, `_listRowHTML`, `_listCardHTML`), `renderer/css/70-detail-misc.css`.
- Material **and Product** cards: **📋 add-to-list / ❤ like / ★ favorite** are now a single **right-aligned inline row** between the illustration and the name (list button first). On the material card it sits **above** the "Burn/Update NFC" banner. Replaces the flags-beside-the-name + the "Add to a list" toolbox row / bottom button — one consistent action row on both cards. `_flagTogglesHTML` + product-card `flagsHTML` gain the list button (owner-only, `.js-add-to-list`); flags moved into a `.pi-flags-row` — `renderer/inventory.js` (`buildPanelHTML`, `_renderProductCard`), `renderer/css/70-detail-misc.css`.
- Header stats now **count-animate** (ticker/odometer roll): on app open they roll up **from zero** (fun to watch the stock + value climb), and later changes tween from the previous value — with a brief green (up) / red (down) tint, instead of a hard flick. Respects `prefers-reduced-motion`. New `_tweenStat` + per-stat `num`/`fmt` — `renderer/inventory.js` (`renderStats`), `renderer/css/00-base.css`.
- Header **"Stock"** stat now shows the weight with **2 decimals** (locale-aware, e.g. "2,67 kg") instead of rounded to the nearest kg — `renderer/inventory.js` (`renderStats`).
- "Add to a list" popup no longer shows a "No lists yet" line when there are none — just the "New list" button. Dropped the now-unused `listNoneShort` key. Its **"New list"** now opens the create-list modal (name + occasion) and adds the item to the freshly-created list, instead of making an untitled list — `renderer/inventory.js` (`_atlPendingAdd`).
- Detail row label `detTwin` renamed "Twin tag" → **"Dual NFC"** (fr "Double NFC") to match the `addProductDualLink` button — NFC-consistent terminology, 9 locales.
- Material (spool) card: **Price + buy link** shown BOTH as a **prominent "Price & buy" block** (big price + clickable buy button, above Details) AND as rows inside **Details** — from your own product record (`_getProduct`), or a friend's shared slice in friend view. New `sectionBuy` i18n + `.det-buy` block — `renderer/inventory.js` (`buildPanelHTML`), `renderer/css/70-detail-misc.css`
- View selector buttons are now **icon-only** — dropped the "Grid / Table / Storage / To order / Cam" text (the group label above already names the context), keeping the ⊞ grid glyph + masked icons; frees horizontal room. Accessible names kept via a new `data-i18n-aria` attribute (localised aria-label only). A **custom hover bubble names each view after a 1s dwell** (CSS `::after` reading the aria-label, delayed fade-in, instant hide — no native title). `btnViewGrid` value lost its `⊞ ` prefix (the glyph is now hardcoded in markup) — `renderer/inventory.html`, `renderer/inventory.js` (`applyTranslations`), `renderer/css/70-detail-misc.css`
- Backfilled the missing **v2.5.0 "What's New"** entry (register 3 — cart, Stock Value + Price column, love⇒auto-reorder, Buy me a coffee; 4 items × 9 locales) — `data/whatsnew.json`. Shows to users in the next release.
- Products view: the ❤ liked / ★ favorite quick-filter buttons now sit **right after the search bar** (start of the filter group), before the attribute filters, instead of at the far right after them — `renderer/inventory.html`
- View selector (Inventory / Favorites / Printers): each group's label now sits **above** its toggle, left-aligned (stacked column) instead of beside it — narrower, frees horizontal room for the action buttons row. Separators stretch to the new height; action buttons align to the toggle row — `renderer/css/70-detail-misc.css` (`.vtg-group`, `.view-toggle-groups`, `.vtg-sep`, `.inv-view-row`)
- Filter dropdowns (right of the search bar, all views): when nothing is selected the collapsed button now shows the **short field name** ("Brand", "Material", "Aspect", "Protocol", "Tag", printer "State") instead of the verbose "All brands"/"All materials"… — frees width. The open-list reset row is shortened to the bare **"All" / "Toutes" / "Tous"** (gender-correct per locale) — still explicit, no longer "All brands". Driven by a new `data-csel-short` (i18n key) read in `_enhanceSelect`; set per-view in `populateQuickFilters` / `populateAspectFilter` / `populateTagFilter` / `populatePrinterFilters` (materialFilter reads "State" in the Printers view). Aspect/Type/Tag select tooltips repointed from the (now bare) `filterAll*` to the `filterShort*` field name — `renderer/inventory.js`, `renderer/inventory.html`
- App-update notification is now a **cloud event** (`type: "announcement"`) instead of an ephemeral local notice: it persists, syncs across devices and sits in the feed like the others (counted, timed, marks read). Deduped once per release via a deterministic doc id `update-<version>` (create-if-missing → no duplicate across devices/restarts). The row opens **What's New** for that version; a **Restart** button is layered on only on the device where the update is actually downloaded (`_updateReadyVersion`). Fires on `update-available`. New `_pushUpdateNotif`; reuses the existing `announcement` type + `data` field — no Firestore rules/schema change — `renderer/inventory.js`

## Added
- **List message to friends.** The Manage-list panel gains an optional free-text **message** (≤500 chars) the owner writes for whoever views the list; it shows as a quoted callout under the list header — for friends in friend-view AND on the public web snapshot. Stored as `message` on `users/{uid}/lists/{listId}` + mirrored into `publicLists/{token}`. Backend: `message` added to both `hasOnly` whitelists + deployed; propagated to Integration (rules + data-model, committed) and TigerHub BRIEF — `renderer/inventory.html`, `renderer/inventory.js` (`openCreateListModal`, `_submitCreateList`, `_createList`, `_syncAllPublicSnapshots`, `renderListsView`), `renderer/css/60-modals.css`, `renderer/css/70-detail-misc.css`, `firestore.rules`. New `listModalMessageLabel`/`listModalMessagePh` i18n.
- **Reorder friends by drag & drop.** Both the **Friends panel** list (grip handle per row) AND the **sidebar friend chips** (left menu) are now draggable; dropping persists a `sortRank` on each `users/{uid}/friends/{fid}` doc (owner-write, batch) and the order applies everywhere friends are shown (panel + sidebar stay in sync). Unranked friends fall back to add order — `renderer/inventory.js` (`_sortFriends`, `_applyFriendReorder`, `_wireFriendDnd`, `_wireSbFriendDnd`, `renderFriendsList`, `renderSidebarFriends`), `renderer/css/20-friends.css`, `renderer/css/00-base.css`. **Both** the sidebar chips AND the Friends-panel rows use a live "make room" animation — the dragged item is lifted (OS drag image follows the cursor) and the others slide (translateY) to open the drop gap, instead of a static insertion line. The panel version also works with a search filter active (operates on the rendered rows, maps the drop to a uid).
- **Racks — same "make room" drag animation.** Dragging a rack (from its head grip) now slides the others to open the drop gap. Uses the shared `_wireMakeRoomDnd` with a new `handleSel` option (drag starts only from the grip, so spools inside a rack stay draggable for storage). `reorderRacks` now applies the new order optimistically (+ re-render) before persisting, so the drop settles cleanly — `renderer/inventory.js` (`_wireMakeRoomDnd`, `reorderRacks`, rack view wiring), `renderer/css/30-racks.css`. Removed the dead `_draggingRackId` + `.rp-rack--drop-*` cue CSS.
- **Printers — same "make room" drag animation** in the **grid view** and the **camera wall**. Dragging a printer now slides the others (2D translate) to open the drop gap instead of the old side-bar cue. Factored into a shared `_wireMakeRoomDnd(host, opts)` helper (items ordered by on-screen position, so it also handles the cam wall's CSS-`order` layout where DOM nodes must NOT move — live WebRTC/iframe streams keep running while cards slide) — `renderer/inventory.js` (`_wireMakeRoomDnd`, `wirePrinterDnd`, `wireCamWallDnd`), `renderer/css/40-printers.css`. Removed the dead `_printerDragId` + `.printer-card--drop-*` / `.cam-wall-card--drop-*` cue CSS.
- **Reorder cart — same "make room" drag animation** (extended to two zones). Dragging a product now slides the others to open the drop gap; the **source** zone closes the dragged item's slot and the **target** zone opens the gap, so moving a product between the active cart and saved-for-later reads fluidly. Replaces the static insertion-line / dashed-zone cues — `renderer/inventory.js` (`_pvHost` drag handlers), `renderer/css/70-detail-misc.css`. Removed the now-dead `_orderDragHash` / `_clearOrderDropCues` and their `.pv-drop-*` / `.pv-zone-drop` cue CSS.
- **Public account auto-accepts friends.** When your account is public, incoming friend requests are now accepted **automatically & instantly, server-side** (Cloud Function `autoAcceptFriendRequestForPublic` in the backend repo — no owner action, works offline): both `friends/{…}` entries are written, the request deleted, and BOTH sides notified (`friend_accepted` to the requester, `friend_added` to the owner). Studio side: the accept/refuse **modal is suppressed on a public account** (the request auto-resolves, so the prompt would be stale), and the new `friend_added` notification renders (avatar + "{name} added you as a friend") — `renderer/inventory.js` (`subscribeFriendRequests`, `_notifText`, `renderNotifCenter`). New `notifAddedFriend` i18n. Backend function + rules deployed; friend-system docs propagated (Integration + Hub).
- **Public lists — shareable web link (Phase 2 write side).** Setting a list to *Public* now mirrors it into a world-readable top-level `publicLists/{token}` snapshot (denormalised, display-only, **no personal note**) so a visitor with **no account** can open it on the web (TigerHub/Vercel). The token is minted + stored on the list (`publicToken`); the snapshot is kept in sync from the lists AND products onSnapshot (signature-cached so it only writes on real change) and deleted when the list leaves Public. The Lists view shows a **right-rail share card** for public lists — QR + a "Copy link" button (no raw URL shown) + **social share buttons (Facebook / X / LinkedIn / WhatsApp / Email)** that open the network's share intent (or a pre-filled `mailto:`) in the browser / mail client, mirroring the friends invite card; the list items sit in a left column beside it. New brand SVGs in `assets/svg/logos/` + `_socialShareUrl` helper + `listShareText` i18n. Backend: new world-readable `publicLists/{token}` rule (owner write, `hasOnly`) + `publicToken` added to the lists whitelist — **deployed**. Base URL is a config const `LIST_PUBLIC_BASE = "https://tigersystem.io"` → links are `https://tigersystem.io/list/<token>`. Propagated to the Integration repo (rules + data-model, committed) and TigerHub BRIEF — `renderer/inventory.js`, `renderer/css/70-detail-misc.css`, `firestore.rules`. New `listSharePublicTitle`/`listSharePublicHint`/`listShareCopied` i18n.
- **List privacy — visible at a glance.** A colour-coded status badge (🔒 Private / 👤 Friends / 👁 Public) now shows next to the list title and as a small icon on each sidebar list, so the visibility is readable without opening the edit modal. Clicking the header badge jumps into the edit modal — `renderer/inventory.js` (`renderListsView`, `_listVisMeta`), `renderer/css/70-detail-misc.css`. New `visPrivateShort` i18n (9 locales).
- **Grouped side card — read-only note** — the deck card now shows the product's note (owner only — a friend's note is never surfaced) so it's visible at a glance without opening the Product-info card. Not editable there — `renderer/inventory.js`, `renderer/css/70-detail-misc.css`.
- **List items — drag-and-drop reorder** (owner only). A grip handle appears on each row (whole row/card is draggable); dropping rewrites the list's `itemKeys` (its display order) with an optimistic re-render, then persists. Works in both the row and grid layouts, with the same live **"make room"** animation as the friends lists & cart — generalised to **2D** so items slide by their (dx,dy) to open the drop gap in the wrapping grid as well as the row list (dragged item lifted, OS drag image follows) — `renderer/inventory.js` (`_lvHost`), `renderer/css/70-detail-misc.css`.
- **List privacy** — the Manage-list panel now has a *Privacy* dropdown (Private / Friends / Public), stored as `visibility` on `users/{uid}/lists/{listId}`. Backend rule updated so `private` = owner-only, `public` = any signed-in user, `friends`/absent = friends (current behaviour) — `renderer/inventory.html`, `renderer/inventory.js`, `renderer/css/60-modals.css`, `firestore.rules` (deployed). Full public web link stays Phase 2.

## Changed
- **Manage-list panel** — the "Delete list" button is now a compact trash **icon** button in the bottom-left of the footer (was a full-width button), still hold-to-confirm — `renderer/inventory.html`, `renderer/css/60-modals.css`.

## Fixed
- **Friend drag order was lost when opening the Friends panel.** `loadFriendsList` (run on every Friends-panel open) rebuilt `state.friends` in Firestore doc-id order, wiping the user's `sortRank` order (which then re-rendered everywhere, incl. the sidebar). Now it sorts by `sortRank`; the localStorage friends cache also carries `sortRank` and is re-sorted on cold-start hydrate, so the order survives restarts with no pre-snapshot flash — `renderer/inventory.js` (`loadFriendsList`, `_cacheFriends`, `_hydrateFriendsCache`).
- CODEMAP drift: `unsubscribePrinters` had moved past the end of the "Racks + printers subscriptions" range — realigned that section's end and the following "Printers views" start to match `renderer/inventory.js` — `renderer/CODEMAP.md`.
- CODEMAP drift (second pass): several anchors (`startMessageInlineEdit`, `doWeightUpdate`, `wireCamWallDnd`, `persistPrinterSortIndices`, `renderRackPresets`, `confirmDeleteRack`, `openAddFriendModal`, `_adfChanged`, `sendFriendRequest`, `generatePublicKey`, `generatePrivateKey`, `syncUserProfile`) had drifted past their mapped section ranges — realigned "TigerTag+ catalogue", "Spool detail panel", "Printers views", "Storage view render + DnD", "Friend view", "Display name + friend requests", "Keys + profile sync" and "Custom avatar" (the last three now abut with no gap/overlap) to match `renderer/inventory.js`; `npm run codemap:check` clean for both `inventory.js` and `main.js` — `renderer/CODEMAP.md`.
- CODEMAP drift (third pass): `_hydrateFriendsCache` (L15722) and `submitPrinterAdd` (L20169) had drifted past their mapped ranges — widened "Friends rendering" end 15505→15790 and "Add-printer flow" end 20000→20330 to match `renderer/inventory.js`; `npm run codemap:check` clean for both `inventory.js` and `main.js` — `renderer/CODEMAP.md`.
- CODEMAP drift (fourth pass): `unsubscribePrinters`, `savePrinterField`, `wireRackTooltipDelegation` and `uploadCroppedAvatar` had drifted past their mapped ranges — recomputed and re-abutted all 14 section ranges from "Panels / debug / auto-update" through "Electron RFID integration" against real anchor positions, and extended the last section to current EOF (25686 lines); `npm run codemap:check` clean for both `inventory.js` and `main.js` — `renderer/CODEMAP.md`.
- CODEMAP drift (fifth pass): the new `_wireMakeRoomDnd` helper pushed `wireCamWallDnd` and `savePrinterField` past their mapped ranges — widened "Printers views" end 17327→17520 and "Printer detail side panel" 17328-19792→17521-19985 (shifted "Add-printer flow" start to 19986 to stay abutting), and extended the last section to current EOF (25834 lines); `npm run codemap:check` clean for both `inventory.js` and `main.js` — `renderer/CODEMAP.md`.
- **Lists view didn't hide when switching to Printers/Rack** — the printer & rack branches of both render dispatchers hid the Products view but not the newer Lists view, so the list stayed on screen with the printers stacked below it — `renderer/inventory.js`.
- Side-card **z-index**: the Product card wrongly painted IN FRONT of the group deck (both were `z 100`, so DOM order decided). Re-numbered the cascade to match `_syncPanels`' left→right order: detail (101) > container/group (100) > **Product (99)** > reorder (98) — so the Product card tucks behind the group as intended — `renderer/css/70-detail-misc.css`.
- Changing a material's **image** now also updates its **Favorites grid/table illustration** (and the product card), not just the material card. The image edit wrote only the inventory (spool) doc; the Favorites read the product snapshot (`label.imgUrl` / `cloudSeed`), which stayed stale. The save now also syncs `label.imgUrl` + `cloudSeed.url_img(_user)` on the product doc when the material is a product (favorite) — `renderer/inventory.js` (`btnCustomImgSave`).
- **"To order" reorder state got stuck.** Un-favoriting a product kept its min-stock, so it stayed in the To-order cart with no way out; and after clearing it, a sticky `savedForLater`/`orderQty` kept it out of the cart forever even when re-favorited with a min. Now: **un-favoriting (★ off) stops reorder tracking** (clears the min → leaves To-order, keeping the "min>0 ⇒ favorite" invariant), and **clearing the min (→0) also clears the reorder-only fields** (`savedForLater`, `orderQty`) in both write paths so a re-added product comes back cleanly — `renderer/inventory.js` (`_coupleFlags`, `_writeProduct`, `_writeProductField`). **Self-heal for already-affected accounts:** on the first products snapshot, `_healProductReorderState` repairs corrupted docs once (clears dangling `savedForLater`/`orderQty` when min ≤ 0; restores `favorite=true` when min > 0 but not a favorite) — no user action needed.
- The header **"Stock value"** stat now updates live when a price changes (`subscribeProducts` calls `renderStats()`) and when the **HT↔TTC price mode** is switched (`saveAccountPriceMode` now repaints the stat, the inventory Price column, and the open card). Added an **ⓘ info bubble** on the stat explaining the value is the remaining filament prorated by weight (price × weight-left ÷ capacity) — new `statValueTip` i18n + `.sb-stat-info` — `renderer/inventory.js`, `renderer/css/00-base.css`
- Editing a filament's **price or buy link** now updates the open **material card live** — the "Price & buy" block and the Details Price/Buy-link rows refresh instantly instead of only on the next open. `_panelCardSig` now includes the price + full buy URL (not just link presence), and `_maybeRefreshPanelForCard` surgically swaps the `.det-buy` block + the Details rows in addition to the toolbox (no full rebuild → image/video preserved). Also covers friend view — `renderer/inventory.js`
- Long-pressing the Friends panel's `»` close tab did nothing (quick click closed it, hold didn't): the hold action ran `_closeAllSidePanels()`, which doesn't include the Friends slide-in, and swallowed the click. The hold now also calls `closeFriends()` — `renderer/inventory.js`


## Removed
- Ephemeral local app-update notice (`_setLocalNotif({ id: "update" })`) — superseded by the cloud notification above — `renderer/inventory.js`

## i18n
- Added: `viewGroupLists`, `listUntitled`, `listCreate`, `listAddTo`, `listRemoveItem`, `listRename`, `listDelete`, `listDeleteConfirm`, `listEmpty`, `listEmptyFriend`, `listNoneTitle`, `listNoneSub`, `listNoneShort`, `listFriendEmpty` — 9 locales
- Added: `filterShortBrand`, `filterShortMaterial`, `filterShortAspect`, `filterShortProtocol`, `filterShortTag`, `filterShortState` — 9 locales
- Added: `notifUpdateCloudTitle`, `notifUpdateCloudText` — 9 locales
- Removed: `notifUpdateAvailable`, `notifUpdateReady` (dead after the move to cloud) — 9 locales
