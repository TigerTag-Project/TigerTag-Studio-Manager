# Worklog — v1.10.16 (in progress)

## Added
- **"What's New" — floating window (draggable + resizable).** The explainer is now a browser-like floating window: drag it by the title bar, resize from the bottom-right grip, and it remembers its size/position (`localStorage tigertag.whatsnew.win`). Restyled — title bar with an accent badge + a version picker pill, a per-version release-note heading ("Tiger Studio Manager X Public Release Note", localised, updates with the picker), items with icons in soft colour chips, sticky footer. `renderer/inventory.html`, `renderer/inventory.js` (`_initWhatsNewWindow`, `_placeWhatsNewWin`), `css/60-modals.css`
- **"What's New" modal (per-version changelog explainer).** After an update, a dedicated window surfaces the new features in vulgarised language, shown **once per version** (compares the running `appVersion` to `localStorage tigertag.whatsnew.seen`). **"Got it"** marks the version seen; **"Later"** / ✕ / backdrop leave it unseen so it returns next launch. Re-openable any time from **Settings → About → "What's New"**. Content lives in `data/whatsnew.json` (keyed by version, **9 locales inline**, full history kept); auto-trigger is skipped while a blocking modal (login / display-name) is open. **Browsable history**: a version picker in the modal lets the user read any past release's notes (newest-first). The full back-catalogue (71 versions) was imported from `CHANGELOG.md` as an **English baseline** (the UI falls back to EN when a locale is missing); v1.10.13–1.10.15 are hand-localised to 9 locales. Release tooling: `npm run whatsnew:add -- <ver>` scaffolds an empty 9-locale entry, `npm run whatsnew:import` backfills all CHANGELOG versions (EN, never overwrites existing localised ones), `npm run whatsnew:check` verifies completeness (EN mandatory; an entry is either EN-only or fully 9-locale — no half-translated states). `scripts/whatsnew-import-changelog.mjs` `renderer/inventory.html`, `renderer/inventory.js`, `css/60-modals.css`, `data/whatsnew.json`, `scripts/whatsnew-add.mjs`, `scripts/whatsnew-check.mjs`

## Changed
- **No more emojis in the UI — replaced by SVG icons or plain text.** Swept the renderer free of true emojis: the "What's New" item icons are SVG icon-set names (not emojis); locale strings (`welcomeBack`, `mobileApp`, `badgeRecycled`, `setupNameSave`, `tutoOpenBtn`, `tigerPodCta`) had their emojis stripped across all 9 locales; `inventory.html` greeting/label/option fallbacks + language-picker flags removed (flags can't be SVG inside native `<option>` → kept the language name); copy button (📋→`icon-copy`) and display-name hero (👋→`icon-user`); printer modules — Bambu humidity (💧→`icon-droplets`), Creality time-left (⏳→`SNAP_ICON_CLOCK`), TigerScale status badges (emoji→text, colour conveys state); plus emoji in comments/CSS. Monochrome functional glyphs (✕ ✓ ▲ ▼ arrows, box-drawing) intentionally left. `renderer/**` (locales, inventory.html, inventory.js, css/70-detail-misc.css, printers/*, IoT/tigerscale)

## Fixed
- **"Set Color & TD Value" modal is now draggable.** Drag it by its header (clamped to the viewport, re-centered on each open). Added a reusable `_makeDraggableModal(overlayId, cardSel, handleSel)` helper. Blur removed behind it + the "TD1S Sensor" and "What's New" overlays (dim only). `renderer/inventory.js`, `css/60-modals.css`
- **Spool grouping: TigerCloud now groups with TigerTag (Maker).** Cloud entries were never grouped (key returned `null`). They're now treated exactly like Maker chips — both use the attribute key `diy:brand|material|colorHex|aspect1` — so a Cloud spool and a Maker spool of the same filament collapse into one group. Only TigerTag+ still groups by `id_product`. `renderer/inventory.js` (`_spoolGroupKey`)

## Removed

## i18n
- Added: `whatsNewTitle`, `whatsNewReleaseTitle`, `whatsNewGotIt`, `whatsNewLater`, `whatsNewEmpty` — 9 locales
