# Worklog — v1.10.12 (in progress)

## Added
- Creality **manual machine controls** on the connected side-panel — set temperatures and move the printer. The K-series runs Klipper + Moonraker, so all commands go through the standard `/printer/gcode/script` endpoint on port 7125 (same host already used for print-start / file-delete). `renderer/printers/creality/index.js`, `renderer/inventory.js`, `renderer/css/55-creality.css`, `main.js`, `preload.js`.
  - **Temperature setpoints**: the nozzle / bed / chamber pills in the temperature card are now click-to-edit (inline numeric input, same UX as Elegoo). Commit sends `M104 S<t>` (nozzle), `M140 S<t>` (bed), or `M141 S<t>` (chamber — the Creality macro, shown only when a chamber temp is reported; it only drives the heater above 40 °C, below that it sets the circulation fan). Targets are applied optimistically; the live push reconciles. Editing is guarded so the ~1.5 s telemetry push can't wipe the field mid-edit.
  - **Control card** (new, hidden during an active print): a jog pad (X/Y/Z + per-axis and all-axis homing) with a step selector (0.1 / 1 / 10 / 30 mm), extrude / retract buttons, a disable-motors button, and **three fans** (Part / Case / Side) as 1%-granular sliders. Jog → `G91 / G1 <axis><dist> F<fr> / G90` (F6000 XY, F600 Z); home → `G28 [axis]`; extrude → `M83 / G1 E<d> F300`; disable → `M84` (all via Moonraker). **Fans** go over the 9999 WS (`creActionFan` → `creSendSet { gcodeCmd: "M106 P<i> S<0-255>" }`, P0/P1/P2 = part/case/side, `S = round(pct/100·255)` so any 1% value works) — the command was reverse-engineered by capturing Creality Print (side fan 42 % → `M106 P2 S107`); my first `{modelFanPct:N}` guess silently no-op'd. Each fan's % is read from the printer's full-state push on connect (now captured in `creMergeStatus`) and updated optimistically; the slider commits on release, the % readout tracks the drag live. Case + side are normally chamber-temperature auto-controlled, so a manual set overrides that. Jog reuses the shared `.elg-*` / `.snap-ctrl-*` styles; the fan rows + extrude/retract/disable are Creality-specific CSS.
  - **Transport** (`creGcode`): routed through a new main-process IPC `cre:http` (`main.js` + `preload.js` bridge) rather than a renderer `fetch`. Moonraker sends **no CORS headers**, so a renderer fetch is blocked (JSON-body POST → preflight `OPTIONS` answered 405; even a simple request's response is unreadable). Node's `fetch` in main is CORS-exempt — same pattern as `snap:http-get` / `ffg:http-post`. The handler has a tight path allow-list (`/printer/gcode/script`, `/printer/print/start`, `/server/files/gcodes/*`) and a 120 s cap (G28 blocks until homing finishes). Both the request and Moonraker's reply go to the live log; Klipper errors (cold extrude, unknown heater…) surface via toast.
  - **Validated on real hardware** (K2 Plus): every control confirmed end-to-end — `M104`/`M140` via `target` read-back, `M141` (chamber macro, heats only >40 °C), `G28` (`homed_axes`→`xyz`), jog `G1` (position delta), extrude (`+5 mm` on the E axis), `M84`, and the fans via `gcodeCmd`/`M106` (side fan capture: `M106 P2 S107`). Corrected two commands found wrong on real hardware: chamber (`SET_HEATER_TEMPERATURE HEATER=chamber` → `M141 S<t>`) and fans (`{modelFanPct:N}` no-op → `gcodeCmd:"M106 P<i> S<0-255>"`). Part=P0 / side=P2 confirmed by capture, case=P1 confirmed in-app on real hardware.

## Changed
- Creality **filament slots are now selectable** instead of opening the edit sheet on click. Clicking a slot with filament **selects** it (the active filament — context for feed / unload); editing moved to a dedicated **pencil button**, which is hidden on RFID-locked slots (the tag defines them). Replaced the ambiguous green "filament present" outline (`.snap-fil--active`) with a clear blue selection ring. Slot state drives everything (per real `boxsInfo`): `state=0` empty, `state≥1` filament present (selectable), `state=2` RFID-read/locked. Three square looks now distinguish **identified** (coloured), **present-but-unidentified** (`--unknown`: solid dashed chip + "?"), and **empty** (striped). The selection defaults to and **re-syncs with** the printer's reported `selected` slot (`conn._selFil` override is dropped whenever the printer's loaded slot changes), so the highlight always tracks what's actually loaded. `renderer/printers/creality/index.js`, `renderer/inventory.js`, `renderer/css/55-creality.css`.
- Creality **CFS Feed / Unload** of the selected slot. When a CFS slot is selected, the filament card shows **Feed** and **Unload** buttons that send the proprietary `feedInOrOut` command (`{boxId, materialId, isFeed}`) over the 9999 WS via `creSendSet` (`creActionFeed`). The printer firmware runs the whole managed sequence itself (auto-heat → cut → feed/retract). **The command was reverse-engineered by MITM-capturing Creality Print ↔ printer 9999 traffic** (decoded from a pktmon pcapng — the slot-feed is *not* reachable via Moonraker; documented in `PROTOCOL.md` §6.6). Feed confirmed on real hardware (K2 Plus, slot 1C → `boxId:1, materialId:2`); unload (`isFeed:0`) inferred from the field name and pending live confirmation.

## Fixed
- Creality **print-start and file-delete were silently broken** by the same Moonraker-CORS issue (renderer `fetch` to `:7125` blocked — JSON-body POST preflight + DELETE preflight both rejected 405). Both now go through the `cre:http` main-process IPC. `renderer/printers/creality/index.js`, `main.js`, `preload.js`.

## Removed

## i18n
- Added: `creControlTitle`, `creExtrude`, `creRetract`, `creDisableMotors`, `creFanPart`, `creFanCase`, `creFanSide`, `creFilEdit`, `creFeed`, `creUnload`, `creFilSelected` — 9 locales
